name: "building dockerfile and publishing"

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

  jobs:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      - name: build dockerfile
        run: |
          docker build . --file Dockerfile --tag goldennews

      - uses
